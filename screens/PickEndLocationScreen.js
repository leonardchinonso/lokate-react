import PickLocation from "./PickLocationScreen";

// PickEndLocation component renders the general pick location component
// with end as action
function PickEndLocation() {
  return <PickLocation action={"end"} />;
}

export default PickEndLocation;
